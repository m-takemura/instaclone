<div class="container">
<div class="wrapper col-sm-6 col-sm-offset-3 ">
  <!-- <% if @photo.errors.any? %>
      <div id="error_explanation">
        <h4><%= @photo.errors.count %>件のエラーがあります。</h4>

        <ul>
        <% @photo.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
   <% end %> -->

  <%= form_for(@photo) do |f| %>
    <%= f.error_messages! %>
    <%= photo_img(@photo) if photo_img(@photo) %>
    <%= f.file_field :picture %>
    <%= f.hidden_field :picture_cache %>
    <%= f.text_area :content, :maxlength => "255", class:"form-control" %>
    <br>
    <%= f.submit "投稿", class:'btn btn-default btn-sm btn-primary pull-right' %>
  <% end %>

  <%= form_for(@photo, url: photos_path, method: 'get') do |f| %>
    <%= f.hidden_field :content %>
    <%= f.submit "戻る",  class: 'btn btn-default btn-sm btn-success pull-left', name: choose_new_or_edit %>
  <% end %>

</div>
</div>
